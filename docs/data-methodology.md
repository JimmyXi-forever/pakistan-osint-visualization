# 数据方法

## 范围

- 时间范围：默认关注最近 7 天。
- 地理范围：巴基斯坦境内恐怖袭击案件；与案件直接相关的跨境反恐行动可作为“关联反恐行动”独立标注。
- 来源范围：优先使用国际通讯社、主流媒体、当地权威媒体、政府或安全机构公开通报。
- 自动候选范围：GDELT 检索到的相关文章只作为候选线索，默认标注为 `reviewStatus: "auto-candidate"`。

## 入库标准

事件进入 `data/incidents.json` 前需要满足：

- 有明确日期。
- 有可定位地点，至少精确到城市或边境区域。
- 有公开来源链接。
- 能区分事件类型，例如“恐袭”“关联反恐行动”“未确认报道”。
- 对伤亡数字存在分歧时，保留区间并在 `fatalities.note` 说明。
- 图片信源需要有原文页面、图片地址、图注、摄影或机构署名；AI 生成示意图和无法确认现场关联的图片不作为证据图片。

## 更新步骤

1. 运行 `npm run fetch:data` 抓取近一周候选报道。
2. 人工阅读来源，排除重复报道、观点文章和无法确认地点的记录。
3. 将确认事件写入 `data/incidents.json`。
4. 若有可靠图片，补充 `media` 字段；若没有，使用 `mediaNote` 说明图片缺口。
5. 运行 `npm run validate`。
6. 刷新页面检查地图、筛选、详情面板和图片信源是否正常。

## 注意事项

涉恐开源情报存在滞后、重复报道和数字分歧。页面中的 `confidence` 字段表示资料可信度，而不是对事件事实的最终裁定。

自动候选的 `confidence` 为“自动候选”，表示尚未完成事实核验。该类记录可用于发现线索，但不应直接用于正式结论。
